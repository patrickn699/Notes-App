apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: authentication
  labels:
    app: auth-svc
spec:
  selector:
    app: auth
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP